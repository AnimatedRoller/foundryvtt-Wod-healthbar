/** Module id must match module.json and flag scope. */
export const MODULE_ID = "wod20-health-monitor";

/** Default box count when no actor is linked or data is invalid (per spec edge cases). */
export const DEFAULT_FALLBACK_BOXES = 7;

export const DEFAULT_BOX_WIDTH = 50;
export const DEFAULT_BOX_HEIGHT = 60;
